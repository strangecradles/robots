# Core (mirror of pyproject dependencies; prefer `uv sync` which uses pyproject)
mujoco>=3.2
numpy>=1.26
pygame>=2.6
opencv-python>=4.10
pandas>=2.2
pyarrow>=17.0
scipy>=1.13
matplotlib>=3.9

# Optional: attention-weighted behavioral cloning (Phase C)
# torch>=2.4

# Optional: 3Dconnexion SpaceMouse teleop (USB or Bluetooth, e.g. Pro Wireless).
# Talks raw HID via hidapi so the Bluetooth link works on macOS/Linux/Windows.
# pyspacemouse>=2.0
# hidapi>=0.14
